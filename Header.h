#pragma once

#include <iostream>
#include <vector>
#include <string>
using namespace std;
int choice;
void options();
vector<string> showUs(vector<string>);
vector<string> addUs(vector<string>);
vector<string> renameUs(vector<string>);
vector<string> deleteUs(vector<string>vec);